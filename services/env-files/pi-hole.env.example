# Admin password for the Pi-hole web interface. Passed to the container as
# FTLCONF_webserver_api_password; v6 renamed it from WEBPASSWORD, which the
# current image silently ignores.
PI_HOLE_WEB_UI_PASSWORD=your-web-ui-password
